# Notes — 20260331_070721_wu32_34_abstract

**Prompt:** `prompts.abstract` · **Model:** gpt-5-mini · **Cost:** $0.2029 · **Timestamp:** 2026-03-31T07:18:42+00:00 · **Run file:** `artifacts/runs/20260331_070721_wu32_34_abstract.json`

**Changes from baseline:** WU-3.2 (VOCABULARY expanded to all 13 data_type + 9 geospatial_info_dataset values with definitions and contrastive examples) + WU-3.4 (MODULATOR_FIELDS time_series negative anchors, threatened_species cue expansion, bias_north_south dual-trigger)

**Baseline comparison (20260327_172654_dev_subset_abstract):**

| Field | Baseline F1 | New F1 | Δ F1 | Baseline P | New P | Baseline R | New R |
|---|---|---|---|---|---|---|---|
| data_type | 0.34 | 0.432 | **+0.09** | 0.24 | 0.365 | 0.58 | 0.528 |
| geospatial_info_dataset | 0.14 | 0.226 | **+0.09** | 0.09 | 0.154 | 0.43 | 0.429 |
| time_series | 0.53 | 0.625 | **+0.10** | 0.36 | 0.455 | 1.00 | 1.000 |
| threatened_species | 0.38 | 0.385 | +0.00 | 1.00 | 1.000 | 0.24 | 0.238 |
| bias_north_south | N/A | 0.182 | **+∞** | N/A | 1.000 | 0.00 | 0.100 |
| species | 0.40 | 0.428 | +0.03 | 0.29 | 0.318 | 0.65 | 0.654 |
| multispecies | — | 0.746 | — | — | 0.759 | — | 0.733 |
| temp_range_i | — | 0.743 | — | — | 0.929 | — | 0.619 |
| temp_range_f | — | 0.629 | — | — | 0.786 | — | 0.524 |
| spatial_range_km2 | — | 0.095 | — | — | 1.000 | — | 0.050 |
| new_species_region | — | 0.080 | — | — | 0.333 | — | 0.045 |
| new_species_science | — | N/A | — | — | N/A | — | 0.000 |

---

### data_type (F1=0.432, P=0.365, R=0.528)
- **Pattern:** F1 improved +0.09 vs baseline. Precision rose from 0.24 → 0.365 (less over-prediction). Recall slightly fell from 0.58 → 0.528, likely because the model now discriminates more carefully but still misclassifies some records. The expanded vocabulary appears to have corrected several wrong-enum predictions where the model was outputting schema-invalid values (e.g., `tracking`, `acoustic`).
- **Root cause:** vocab gap (partially addressed)
- **Recommendation:** Target F1 ≥ 0.50 not yet met. Inspect remaining mismatches — likely `presence-only` vs `presence-absence` confusion and multi-label cases where a single dominant type should be selected. May benefit from an explicit instruction to pick the single best-fit label when data covers multiple types.

### geospatial_info_dataset (F1=0.226, P=0.154, R=0.429)
- **Pattern:** F1 improved +0.09 (0.14 → 0.226), mostly through precision doubling (0.09 → 0.154). Recall held steady at ~0.43. The negative rule ("named places in background text do not qualify") reduced false positives from place-name mentions, but precision is still low — the model continues to over-predict `administrative_units` and `site` from location context.
- **Root cause:** prompt (partially addressed)
- **Recommendation:** The vocabulary expansion helped but the scoping rule needs reinforcement. Consider adding an explicit instruction near the top of the task block: "Geospatial info describes the *dataset structure*, not the study geography — look for coordinates, site ID tables, or explicit distribution layers in the data." The distribution/sample contrastive example may need concrete abstract-level phrasing.

### time_series (F1=0.625, P=0.455, R=1.000)
- **Pattern:** Largest win of this round: F1 +0.10, precision +0.09 (0.36 → 0.455). Recall remains 1.00. The negative anchors (multi-year windows ≠ time series, compilation ≠ repeated monitoring) are working. Target precision ≥ 0.55 not yet met but clearly trending the right direction.
- **Root cause:** prompt (partially addressed)
- **Recommendation:** Review remaining false positives — likely cases where multi-year study windows appear with survey language (e.g., "biannual surveys 2010–2014") that the model still fires on even without explicit same-site revisit language. May need one more round of negative examples with specific phrasing from actual mismatches.

### threatened_species (F1=0.385, P=1.000, R=0.238)
- **Pattern:** No improvement from baseline. Precision remains perfect, recall unchanged at ~0.24. The expanded cue list (IUCN, SARA, COSEWIC, CITES, provincial red lists) did not improve recall on this subset.
- **Root cause:** Likely not a prompt issue — the GT positives may use language not in the abstract at all (e.g., species is known to be threatened but abstract doesn't mention conservation status). OR the model is being over-conservative with the "use ecological knowledge only when certain" qualifier.
- **Recommendation:** Inspect the 8+ missed GT positives: do the abstracts contain any of the expanded cue list terms? If no cues appear in the abstract text, this field may require sections mode (introduction/discussion often contains conservation framing). If cues are present and being missed, the model is not reading them correctly — try removing the "only when certain" caveat for ecological inference.

### bias_north_south (F1=0.182, P=1.000, R=0.100)
- **Pattern:** Went from R=0.000 to R=0.100 — 1 positive case out of 10 now correctly identified. Perfect precision (no false positives yet). The dual-trigger structure is working at a basic level. The geographic trigger (Nunavik, northern Quebec, above 49°N) caught at least one case; explicit bias language trigger is possibly catching fewer than expected.
- **Root cause:** prompt (partially addressed — geographic trigger working, still low recall)
- **Recommendation:** Inspect which GT positives are being missed: are they geographic (study area in northern QC but abstract uses other terms) or bias-language (discussed sampling gaps but not in abstract)? If most GT positives are geographic but abstract-level language is indirect, sections mode may be needed. Also verify whether the `bias_north_south` GT positives all have clear abstract-level signals or if this is a field where abstract alone is insufficient.

### species (F1=0.428, P=0.318, R=0.654)
- **Pattern:** Minor improvement +0.03 F1. Not directly targeted in this round (WU-T1 covers species). Precision still low (0.318) — context/decorative species still leaking through.
- **Root cause:** prompt (not addressed this round — deferred to WU-T1)
- **Recommendation:** No action this round. Species precision will be addressed in WU-T1 (SPECIES_EXTRACTION block).

---

## Sections and PDF Native Modes

**Status:** Could not run in this environment — PDF files at `data/pdfs/fuster/` are not available locally. All 30 records returned extraction errors (`FileNotFoundError`). Baseline comparisons for sections and pdf_native modes remain as established in the March 27 runs.

**Impact of WU-3.4b changes (section.py + pdf_file.py):** The mode-specific rules were applied as planned but cannot be validated until the next session with PDFs available (devcontainer or cloud environment).
