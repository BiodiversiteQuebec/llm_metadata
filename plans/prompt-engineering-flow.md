Here are some observations and ideas.

* Running full batch extraction in bad prompts results in expensive querying and bad resutls - prompt engineering should happen before batch extraction and evaluation. See latest abstract batch notebook results in README.md and latest batch full_text results.
* Prompt engineering should be done on a feature basis - FieldMetrics matter.
* Prompt engineering should be done on a representative subset of the whole validated dataset ? What would that be like. Probably reusing the preselected records ovre again so I or claude to guide next improvements.
* Is the notebook the right abstraction to iterate on prompts and evaluation ? Good alternative ? Ie. versionned prompt + schemas + pytest ? Weigh in.
* Should have agentic workflow supported by tasks with different models (opus for results review and improval proposal, sonnet / haiku to run extract/eval). What would that flow be like ? Human in the loop is important, and so is letting the space to CLAUDE to do its magic.
* I don't want to refactor my whole workflow, bu I'm open to good practice.
* Evaluation method is a bottleneck. Some field results are bad because the evaluation method is bad (would benefit from llm-as-judge method), some are just irrelevant and drag the overall scores down, some extract/eval should benefit by restricting the evaluated fields... may be we should start with that and harden our evaluation workflow before prompt engineering and batch ?
* I already know that some extraction is having a hard time scoping what information is relevant within an abstract / paper. There is the dataset (or multiple ones ?) that are within the article scope. Example  :biological data is acquired on the terrain for n species, then simulations or populations are generated. Other datasets are sometimes cited because they are not generated by or for the study, but comes from previous studies to contextualize results or be integrate in the methodology. These unoriginal datasets should be excluded from extracted information.
* This affects a whole lot of files in our project.