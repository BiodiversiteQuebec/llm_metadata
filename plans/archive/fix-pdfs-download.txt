Many pdfs that were downloaded for the fuster analysis in data/pdfs/fuster are invalid. (Less thant 100 kb, can't be opened by acrobat reader). Probably that the download link were not referring directly to a pdf file, but a html or something like that.

Your goal is to fix the multi-step download process and perform all the download for the fuster article.

Here are some details.

* Find and delete all 
* Create a ./scripts/download_all_fuster_pdfs.py from related notebook. Ignore ones from zenodo.
* Implement a assert in download functions to check if pdf file is valid.
* Create an integration test (unittest) to run in and make it past.

* Break only on one of these conditions
    * Insufficient access to files/website to create/download/remove files.
    * Need a complete refactoring of existing modules (ie. Replace a module dependancy) or significant deviation from the plan.
    * Need an unplanned implementation (ie. new logic module, new dependancy)
    * You achieve the goal.